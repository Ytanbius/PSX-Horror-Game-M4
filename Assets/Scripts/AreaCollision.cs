using UnityEngine;

public class AreaCollision : MonoBehaviour
{
    public Object cameraArea;
    private Object curCamera;
    //Eu não entendo oque eu escrevo
    //private void OnTriggerEnter(Collider other)// Não sei se é melhor Enter ou Stay
    //{
    //    curCamera = other.GetComponent<CameraSwitch>().curCamera;
    //    if (curCamera != cameraArea ) //Acho que deve ser por esse caminho
    //    {

    //        other.GetComponent<CameraSwitch>().curCamera = cameraArea;

    //    }
    //}
}
